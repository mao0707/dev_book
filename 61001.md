* 部门同步请求地址
```
http://oa.eol.com.cn/api.php?mod=topdepartment
```
* 用户同步请求地址
```
http://oa.eol.com.cn/api.php?mod=topadmin
```

使用方法：
```PHP
$config = array( 'error' => FALSE, 'debug' => FALSE, 'log' => FALSE, ); // $this->load->library("EXT_Sync", $config, 'oSync'); // $this->oSync->topAdmin(); // $this->oSync->topDepartment();

 $this->load->model('sys/logic_dzz', 'oDzz'); $this->oDzz->initialize($config); // var_dump($this->oDzz->topDepartment()); $this->oDzz->topAdmin();
```
